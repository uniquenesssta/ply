## 自动工具调用协议

执行本任务书时，允许并要求 AI 根据实际需要自动调用已安装插件，不需要用户逐次指定。

### Context7
满足以下任一条件时自动调用：
- 使用不熟悉或可能已更新的第三方库/API；
- 出现废弃接口、方法不存在、版本不兼容或编译错误；
- 新增涉及框架、依赖库或系统API的功能；
- 当前实现方式可能受具体版本影响。

调用要求：
- 先确认项目中的真实依赖版本；
- 查询对应版本的文档；
- 每次只查询一个明确问题；
- 不向插件发送密钥、密码、用户数据或大段私有源码；
- 查询结果必须结合当前代码验证，不能直接照搬。

### Mermaid Chart
满足以下任一条件时自动调用：
- 修改跨越三个及以上模块；
- 涉及复杂数据流、状态机、线程、事件或生命周期；
- 新增或调整重要架构；
- 任务书中的文字关系难以直接理解；
- 用户要求全链路梳理。

调用要求：
- 创建或更新对应项目的架构图；
- 图中使用中文名称；
- 只表达真实代码链路，不绘制尚未实现的功能；
- 小型、单文件或简单修改不调用。

### Create State
满足以下任一条件时自动调用：
- 一个阶段或里程碑完成；
- 作出重要架构决定；
- 依赖版本、数据结构或接口协议发生变化；
- 复杂Bug已经定位或修复；
- 本轮工作即将结束，需要下次继续。

保存内容：
- 项目名称与当前版本；
- 已完成任务；
- 修改的关键文件和模块；
- 测试、编译及验证结果；
- 关键架构决定及原因；
- 已知问题与失败尝试；
- 下一步任务；
- 后续不得破坏的约束。

Create State只保存项目状态和开发决定，不替代Git、README和任务书。

### 执行优先级
1. 读取任务书和现有代码；
2. 确认当前版本及工作区状态；
3. 必要时调用Context7；
4. 制订最小完整修改方案；
5. 架构复杂时调用Mermaid Chart；
6. 修改代码；
7. 执行测试、编译和差异复核；
8. 更新README与任务书状态；
9. 达到保存条件时调用Create State；
10. 汇报本轮结果及插件使用情况。

### 失败处理
- 插件不可用时不得阻塞可以继续完成的代码工作；
- 不允许根据记忆伪造插件查询结果；
- 插件调用失败时记录失败原因并使用官方文档或现有代码继续；
- 涉及架构、版本兼容或关键状态保存失败时，在最终结果中明确说明。
