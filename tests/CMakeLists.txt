# Test targets are added alongside the first independently testable module.
# Keeping this file now makes the top-level build/test entry points stable
# without introducing placeholder test code or mock-only verification.
