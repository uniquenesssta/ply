# Qt6 + libmpv 播放器阶段实施任务书

> 版本定位：快速框架实施版  
> 范围：R2–R14  
> 原则：先把正确的模块框架和真实主链跑起来，再集中修复、补边界和做强验证。  
> 不包含：R0、R1；现有 R0/R1 状态不在本包中重新定义。

## 统一执行策略

本任务书不要求每个 Atomic Task 都先写成“工程合同”。普通任务只要明确目标、模块、主链、实现重点和基本验证即可直接开工。

整个项目按四个节奏推进：

1. **骨架优先**：先建立真正会被使用的目录、CMake、接口、对象所有权和最小实现，不创建大批空文件。
2. **主链优先**：优先让真实链路工作，例如 `libmpv → PlaybackSession → Render → ViewModel → QML`，先看到结果。
3. **边做边修**：编译错误、接口不顺、职责耦合、明显竞态、真实运行问题出现后立即在影响边界修复；普通边缘问题可记录到阶段修复清单，不要求每个小任务都阻断后续。
4. **集中硬化**：R12 专门负责压力、故障、性能、泄漏、长时间运行和最终架构审查；R13 再做真正发布门禁。

只有以下问题默认视为“必须先停下来解决”的硬阻断：

- 项目无法 configure/build；
- 主链完全不可运行；
- 明确的状态双重拥有、循环依赖或错误模块边界；
- crash、deadlock、use-after-free、线程越界等高风险生命周期问题；
- 数据损坏、不可逆迁移错误；
- Render/mpv 销毁顺序错误；
- 发布阶段安装包无法在干净机器启动。

普通 UI 细节、非主路径边缘场景、尚未覆盖的手工矩阵不需要阻止每个 Atomic Task 继续，但必须在阶段修复清单或 R12/R13 前关闭。

## 软件可用性里程碑

| 里程碑 | 达到的实际形态 |
|---|---|
| R2 | 无 UI 的 libmpv 播放核心可真实加载/播放/Seek/关闭 |
| R3 | 播放状态、线程、命令/事件稳定，形成真正“播放器内核” |
| R4 | 视频真正进入 Qt Quick，开始像一个播放器 |
| R6 | 主窗口、播放/暂停、Timeline、音量、全屏可用，形成基础桌面播放器 |
| R8 | 文件/URL/播放列表/音轨/字幕/章节完整，达到主流播放器核心功能形态 |
| R10 | 历史/设置/快捷键/媒体键/单实例/文件关联齐全，Windows 日常使用基本完整 |
| R11 | 高级能力增强 |
| R12 | 稳定性和性能收敛 |
| R13 | 可发给别人安装使用的 Windows 正式桌面软件 |
| R14 | macOS/Linux 平台适配 |

## 模块化总原则

- 一个模块只拥有一个清晰职责；新能力有独立状态、IO、生命周期、错误或测试边界时再拆文件/目录。
- 不为了“文件多”而拆；也不为了“改得少”把不同职责继续塞进同一个文件。
- QML 不直接调用 libmpv；PlaybackSession 是播放真值；Playlist 只拥有队列；DatabaseWorker 只拥有数据库连接；Renderer 只拥有 Render/OpenGL 资源。
- 所有源码/配置/行为变化继续同步记录根 `README.md`。
- R0-06 已按用户要求跳过，因此依赖合法可分发媒体 fixture 的验证，如果只有本地合法样本，必须写成“本地验证”，不能伪装成完整 fixture 体系已经闭环。

# Stage R12：可靠性与性能硬化

## 1. 本阶段最终要得到什么

这是项目真正“收敛”的阶段。前面允许快速建框架、边跑边修；到了 R12 才集中用压力、故障和长时间数据把软件打磨到发布质量。

## 2. 本阶段怎么快速推进

R12 不按每个小功能继续扩张，而是先跑测试暴露真实问题，再按根因修。优先 crash/UAF/deadlock/数据损坏，其次性能和资源，再处理边缘兼容。

## 3. 目标模块结构

```text
tests/stress/
tests/integration/
tests/platform/
tools/diagnostics/
tools/perf/
现有各模块（仅在真实根因影响范围内修改）
```

## 4. 阶段主链

```text
Stress/Fault/Perf Harness
  ↓
Real Application Chain
  ↓
Metrics + Logs + Reproduction
  ↓
Root-cause Fix
  ↓
Targeted Regression + Stage Regression
```

## 5. 关键状态/资源归属

R12 不新增第二份状态；所有诊断只观察现有 owner。测试代码也不能通过 mock 替代必须验证的真实 mpv/render/db 链。

## 6. Atomic Tasks

### R12-01 快速媒体切换压力

**目的**  
集中打快速 A/B/C load、seek、pause。

**主要模块 / 文件**  
stress tests

**主要链路**  
`stress harness → app → metrics/logs`

**实施重点**
先自动循环 100 次，发现真实 stale state 后修；再提高次数。

**基本验证**
0 crash/UAF；旧 generation 不污染当前。

**完成判断**  
快速切换稳定。

### R12-02 完整生命周期压力

**目的**  
做 100 次应用/Session/Render 创建销毁。

**主要模块 / 文件**  
lifecycle stress

**主要链路**  
`start → play → close ×100`

**实施重点**
记录 thread/handle/memory 趋势。

**基本验证**
无持续增长/死锁。

**完成判断**  
生命周期稳定。

### R12-03 损坏媒体矩阵

**目的**  
验证坏容器/截断/权限/不支持等。

**主要模块 / 文件**  
fixtures/integration

**主要链路**  
`bad input → expected error state`

**实施重点**
合法 fixture 不完整时允许本地样本，但要记录来源。

**基本验证**
不无限 loading、不 crash。

**完成判断**  
错误链稳定。

### R12-04 网络超时与断流

**目的**  
验证不可达、慢、断流、取消。

**主要模块 / 文件**  
test server/network harness

**主要链路**  
`network scenario → playback states`

**实施重点**
重点看关闭和取消是否挂住。

**基本验证**
timeout/断流有明确状态。

**完成判断**  
网络异常可恢复。

### R12-05 4K/高帧率性能

**目的**  
以数据修性能，不凭感觉。

**主要模块 / 文件**  
perf diagnostics

**主要链路**  
`4K/HFR → CPU/GPU/frame/drop/UI metrics`

**实施重点**
固定硬件和样本记录 baseline/after。

**基本验证**
达到目标或明确瓶颈。

**完成判断**  
有可追踪性能结论。

### R12-06 长时间播放

**目的**  
跑 8h/24h。

**主要模块 / 文件**  
long-run harness

**主要链路**  
`long playback → periodic metrics`

**实施重点**
看内存、句柄、日志、时钟。

**基本验证**
无明显爬升/溢出。

**完成判断**  
长时间稳定。

### R12-07 内存与资源泄漏

**目的**  
使用 ASan/VS diagnostics/handle counters。

**主要模块 / 文件**  
debug/asan tooling

**主要链路**  
`scenario → resource snapshots → diff`

**实施重点**
只修确定根因，不为第三方噪声大改架构。

**基本验证**
无确定持续泄漏。

**完成判断**  
资源边界清晰。

### R12-08 DPI、多屏、全屏压力

**目的**  
集中测各种窗口/显示器转换。

**主要模块 / 文件**  
window/render stress

**主要链路**  
`monitor/DPR/fullscreen/minimize loops`

**实施重点**
重点看 FBO、geometry、context。

**基本验证**
无持续黑屏/context error。

**完成判断**  
多屏稳定。

### R12-09 数据库故障

**目的**  
模拟 locked/read-only/写失败/坏库。

**主要模块 / 文件**  
persistence fault tests

**主要链路**  
`fault → DB worker → degrade/error`

**实施重点**
确认不会阻塞 Playback。

**基本验证**
播放不崩，flush 有界。

**完成判断**  
持久化故障可诊断。

### R12-10 配置损坏恢复

**目的**  
模拟坏 JSON/非法设置。

**主要模块 / 文件**  
settings fault tests

**主要链路**  
`bad setting → fallback/diagnostic`

**实施重点**
默认值和坏项隔离。

**基本验证**
可启动可恢复。

**完成判断**  
配置损坏不拖垮应用。

### R12-11 诊断包导出

**目的**  
把日志、版本、依赖、关键设置收集并脱敏。

**主要模块 / 文件**  
diagnostics exporter

**主要链路**  
`collect → redact → package`

**实施重点**
重点防 token/私人路径泄漏。

**基本验证**
导出后人工检查。

**完成判断**  
诊断包可用于定位问题。

### R12-12 最终架构审查

**目的**  
在功能已经成形后再做一次真正模块化审查。

**主要模块 / 文件**  
whole repo

**主要链路**  
`dependency/state/file responsibility review → targeted refactor`

**实施重点**
只拆确认有问题的耦合，不做全项目美化重写。

**基本验证**
build/test + 关键链回归。

**完成判断**  
无已知严重重复状态/循环依赖/巨型多职责模块。

## 7. 框架打通后优先修复

这一阶段本身就是修复阶段：每个失败都形成“复现 → 根因 → 修复 → 回归”。不追求改动最少，而追求根因闭环。

## 8. 本阶段最小可运行里程碑

连续日常使用、快速切换、长时间播放、4K、异常媒体、网络/数据库故障均不出现发布阻断级问题。

## 9. 阶段关闭条件

所有已知 crash/deadlock/UAF/data corruption 关闭；性能有基线；100 次生命周期通过；发布候选的功能集合冻结。剩余非阻断限制必须记录。

## 10. 本阶段暂不要求

R12 不新增大功能；发现功能设计缺失时回对应模块补最小必要能力。

## 11. 后续扩展位置

R12 的自动化场景后续可成为 nightly/Release CI。
