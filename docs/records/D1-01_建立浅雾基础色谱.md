# D1-01 建立浅雾基础色谱 — 完成记录

> Task ID：D1-01  
> 状态：Complete  
> Figma 文件：`Qt6 + libmpv Player — Native Blank Slate Exploration`  
> Figma File Key：`KIOxfwTvQJlcVLinkeJAxY`  
> Foundations Page：`01 Foundations`  
> 色谱板节点：`8:3` — `D1-01 / Primitive Color Palette`

## 1. 本次完成内容

从已确认的第三版三张核心框架中提取实际使用的基础色值，并建立独立 Primitive Color 变量集合。

新增 Figma Variable Collection：

- `V3 / Primitive`
- Mode：`Base`

新增 15 个 Primitive Color：

- `color/neutral/white` — `#FFFFFF`
- `color/neutral/mist-50` — `#F7F7FC`
- `color/neutral/mist-100` — `#EDEEF7`
- `color/neutral/ink-950` — `#1A1720`
- `color/neutral/ink-900` — `#252029`
- `color/neutral/ink-700` — `#706A78`
- `color/neutral/ink-600` — `#76707B`
- `color/neutral/ink-500` — `#8C8491`
- `color/violet/700` — `#6A2CFF`
- `color/violet/600` — `#7B2CFF`
- `color/violet/400` — `#9A5CFF`
- `color/violet/100` — `#E7D8FF`
- `color/cyan/400` — `#56C8FF`
- `color/warm/400` — `#FFB26A`
- `color/shadow/plum` — `#2E1F47`

所有 Primitive Color 均设置为空 scopes，避免污染普通属性选择器；同时写入稳定的 WEB code syntax 标识，便于后续 Handoff 映射。

## 2. Figma 可视化

在 `01 Foundations` 中新增：

```text
D1-01 / Primitive Color Palette
└─ Swatch Grid
   ├─ Row 1
   ├─ Row 2
   └─ Row 3
```

色卡直接绑定对应 Primitive Variable，而不是复制硬编码色值。

## 3. 验证

已执行：

- 对现有 `Framework / Main Player`、`Framework / Fullscreen`、`Framework / Playlist Inspector` 做真实 fill/stroke/effect 取样。
- 确认基础色谱来源于现有第三版框架，不另造传统 Brand Blue 色板。
- Figma Variables 创建成功：1 collection / 1 mode / 15 color variables。
- 第一次截图发现 Swatch Row 高度误为 `1 px`，色卡被裁切；随后修正 Row 为 `168 px`、Grid 为 `540 px`。
- 第二次截图复核通过：15 个色卡全部可见，标签和 HEX 值完整。

## 4. 保持不变

本任务没有：

- 创建 Semantic Color；
- 修改 Main Player / Fullscreen / Inspector 的现有视觉；
- 建立组件库；
- 引入外部 UI Kit；
- 改变第三版已确认视觉方向。

## 5. 下一任务

`D1-02 建立语义颜色`

目标：将本次 Primitive Color 映射到 `surface / text / border / accent / selection / focus / feedback / control / inspector` 等 Semantic Color，并开始让成品框架消费语义变量，而不是直接依赖 Primitive。