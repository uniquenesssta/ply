# D7-04 窄窗口降级

> Stage：D7 窗口模式与响应式行为  
> Task：D7-04 窄窗口降级  
> Scope：Figma 设计契约与验证；不代表 Qt/QML runtime 已实现。

## Figma

```text
Page                     382:5777  07 Window Modes
Source / Contract        407:47    D7-04 / Narrow Responsive Policy
Verification             407:48    D7-04 / Verification
Policy Matrix            408:61    Responsive Policy Matrix
Pressure Ladder          408:95    Narrow Pressure Ladder
Inspector Contract       408:119
Overflow Contract        408:124
Acceptance Gate          408:129   D7-04 / Acceptance Gate
Geometry Audit           413:203   Geometry / Ownership Audit
Verification Assertions  413:223   D7-04 / Verification Assertions
```

## Authority / breakpoint contract

D7-04 不建立第二套 responsive breakpoint。D2-05 继续是唯一全局断点 owner：

```text
Narrow     0–839
Standard   840–1199
Wide       >=1200
```

本任务使用 `820 / 720 / 640 / 560` 作为 Narrow 内的 QA 压力宽度；它们不是新的全局 breakpoint，也没有对应的新 Responsive Mode 或 Variable。

共享 authority 保持：

```text
D2-05 Responsive Window Skeleton        79:2
D3-02 Timeline Basic Geometry           95:8
D3-05 Volume Cluster                    127:2
D3-06 Utility Action Cluster            134:2
D4-07 Inspector Responsive Contract     207:2497
```

## Collapse priority

```text
Wide
  Utility full 222×32
  Volume inline 138×32
  Current + Duration
  Inspector dock 368

Standard
  Utility mixed 146×32
  Volume trigger 32 + z60 Popover
  Current + Duration
  Inspector overlay 368

Narrow
  Utility essential+More 108×32
  Volume trigger 32 + z60 Popover
  Inspector overlay 320
  Timeline information yields before geometry
```

Narrow 的直接可见 Utility 固定为：

```text
Subtitles
More
Fullscreen
```

低优先级动作继续通过既有 More Popover 可达：

```text
Audio Tracks
Chapters
Playlist
```

Volume 不被塞进 More。Narrow / Standard 仍使用 D3-05 的独立 Volume Trigger + Popover。

## Interaction geometry

空间压力不能通过缩小交互目标解决：

```text
Transport        116×40
PlayPause         40×40
Volume Trigger    32×32
Utility           108×32 in Narrow
Timeline Hit      16px
Timeline Track     3px
Timeline Thumb    10×10
```

上述几何在全部 D7-04 Narrow 验证宽度保持不变。

## Timecode pressure rule

D3-02 的 seek 几何优先于第二个时间码。D7-04 冻结 Narrow 内部的局部 pressure rule：

```text
Timeline Lane >= 480px
  → Current + Duration

Timeline Lane < 480px
  → Current only
  → Duration yields first
```

`480px` 是 Timeline Lane 的局部信息密度压力规则，不是第四个全局 Window breakpoint，也没有建立新的全局 token。

在当前 Narrow host 几何下：

```text
Player 820 → OSC 716×106 → lane 660 → Current + Duration
Player 720 → OSC 616×106 → lane 560 → Current + Duration
Player 640 → OSC 536×106 → lane 480 → Current + Duration
Player 560 → OSC 456×106 → lane 400 → Current only
```

## Header pressure rule

Compact Header 不缩小 Title Pod 或 Window Actions：

```text
Media Info Pod   360×50
Window Actions   110×50
```

空间受压时只改变 placement。560 verification：

```text
Title   x26  width360
Actions x424 width110
Gap     38
```

因此没有通过缩小 window action hit target 解决碰撞。

## Real verification

```text
820 Narrow Upper       410:49   window 820×700   OSC 716×106   lane 660   time pair
720 Narrow Reference   410:108  window 720×700   OSC 616×106   lane 560   time pair
640 Narrow Floor       410:167  window 640×700   OSC 536×106   lane 480   time pair
560 Narrow Stress      411:203  window 560×700   OSC 456×106   lane 400   Current only
560 More Open          411:261  window 560×700   More 250×176 @ 284,360
560 Inspector Open     411:323  window 560×700   Inspector 320×450 @ 214,88
```

### 560 More

- 复用 D3-06 既有 `More Popover` source `134:273`。
- Popover=`250×176 @ 284,360`。
- 完整落在 560 窗口内，并位于 OSC y564 上方。
- Audio / Chapters / Playlist 保持可达。
- More 继续是 z60 overflow owner，不创建第二个 Inspector。

### 560 Inspector

- 复用 D4-07 真实 `Inspector / Shell` Instance，main component 仍为 `Inspector / Shell`。
- Shell=`320×450 @ 214,88`。
- Inspector bottom=`538`，OSC top=`564`，因此 Inspector 停在 OSC 上方。
- OSC 保持 `456×106`，没有因 Narrow Overlay 打开而收缩。
- Video Viewport 仍不因 Inspector open/close 改变。

## Verification result

最终结构审计：

```text
Source Section fit                    PASS
Verification Section fit              PASS
Visible unbound product paints        0
Generic unnamed residues              0
D7-04 formal Components               0
D7-04 Reactions                       0
D7-04 AFTER_TIMEOUT owners            0
New D7-04 global Variables            0
Section outline residue               0

820 title/action gap                  94
720 title/action gap                  44
640 title/action gap                 118
560 title/action gap                  38

All cases Timeline hit                16
All cases Timeline track               3
All cases Timeline thumb              10×10
All cases Transport                   116×40
All cases PlayPause                    40×40
All cases Volume                       32×32
All cases Narrow Utility              108×32

560 More inside host                  PASS
560 More above OSC                    PASS
560 Inspector main                    Inspector / Shell
560 Inspector stops above OSC         PASS
560 Inspector above OSC z-order       PASS

D2-05 authority regression            PASS
D3-02 authority regression            PASS
D3-05 authority regression            PASS
D3-06 authority regression            PASS
D4-07 authority regression            PASS
D7-01 / D7-02 / D7-03 geometry        PASS
Acceptance Gate                       10 / 10 PASS
Verification Assertions               12 / 12 PASS
```

D7-04 verification 中复制的 Utility SVG 默认 `Vector` 子路径只在新副本内做职责化命名；D3 source 没有修改。最终 generic residue=`0`。

## Runtime boundary

当前 `agent/r4-stage` 的实际 QML 仍处于：

```text
PlayerScreen
  → VideoSurface
  → PlayerChrome
```

`PlayerChrome` 仍是早期 placeholder chrome；尚无 D7-04 Narrow responsive runtime owner、真实 collapse resolver 或 Inspector/More 窄窗运行时布局。因此本任务完成的是 Figma 设计契约，不将其描述为源码功能已经实现。

本任务没有修改播放器源码、配置、依赖、数据格式或运行时接口；无构建、单元测试或运行时测试项。

**D7-04：Complete。**